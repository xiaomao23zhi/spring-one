spring:
  application:
    name: spring-one

  cloud:
    nacos:
      config:
        server-addr: ${NACOS_CONFIG_SERVER:localhost:8848}
        group: ${NACOS_GROUP:DEFAULT_GROUP}
        file-extension: yaml
      discovery:
        server-addr: ${NACOS_DISCOVERY_SERVER:localhost:8848}
